# Progress log

The agent's record of what happened. One row per step: what was done, which
commit holds it, and how it was checked, including what you confirmed.

Do not edit this file. The agent appends to it.

`Status` is `done`, `skipped` or `failed`.

| Step | Status | What happened | Commit | Verification |
|------|--------|---------------|--------|--------------|
